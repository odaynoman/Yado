{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the widget window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "notification:default"
  ]
}
